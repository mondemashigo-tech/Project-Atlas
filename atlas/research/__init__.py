"""Domain research modules. Each domain (fx, ...) is a self-contained
research engine that Atlas orchestrates. The kernel and council treat these as
deterministic tools that produce evidence, never as decision-makers."""
